apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: user-db-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: user-db-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_HOST
      remoteRef:
        key: microservices/user-db
        property: host
    - secretKey: DB_PORT
      remoteRef:
        key: microservices/user-db
        property: port
    - secretKey: DB_NAME
      remoteRef:
        key: microservices/user-db
        property: name
    - secretKey: DB_USER
      remoteRef:
        key: microservices/user-db
        property: user
    - secretKey: DB_PASSWORD
      remoteRef:
        key: microservices/user-db
        property: password
